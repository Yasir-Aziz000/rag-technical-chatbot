# ── Core LangChain ───────────────────────────────────────
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-groq>=0.2.0,<0.4.0
langchain-huggingface>=0.1.0,<0.2.0

# ── Embeddings & Models ──────────────────────────────────
sentence-transformers>=3.0.0,<4.0.0

# ── Vector Store ─────────────────────────────────────────
# langchain-chroma dropped: it pins old numpy that fails to build on Python 3.14.
# langchain-community has an equivalent Chroma wrapper with no such constraint.
chromadb>=0.6.0

# ── Document Parsing ─────────────────────────────────────
pdfplumber>=0.10.0,<0.12.0
python-docx>=1.1.0,<2.0.0

# ── Config & Validation ──────────────────────────────────
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# ── Logging ──────────────────────────────────────────────
loguru>=0.7.0,<0.8.0

# ── Frontend ─────────────────────────────────────────────
streamlit>=1.40.0,<2.0.0
